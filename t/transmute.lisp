(in-package :cl-user)
(defpackage transmute-test
  (:use :cl
        :transmute
        :prove))
(in-package :transmute-test)

;; NOTE: To run this test file, execute `(asdf:test-system :transmute)' in your Lisp.

(plan nil)

;; blah blah blah.

(finalize)
